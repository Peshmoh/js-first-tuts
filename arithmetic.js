/*arithmetic expression is a combination of
uperands(values,variables,e.t.c)
operator(+ - * / %) that can be evaluated to a value ex.y = x + 5;
 */

//let students = 20;

//students = students + 1;
//students = students - 1;
//students = students * 2;
//students = students / 2;
//let extrastudents = students % 2;

//students += 1;

let result = 1 + 2 * (3 + 4);

console.log(result);